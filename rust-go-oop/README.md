# OOP with many languages

An `Shape` (with `area` and `perimeter`) interface implemented in
`Dart`, `Go` y `Rust`.

# Running

With Dart:

```
dart shape.dart
```

With Go:

```
go run shape.go
```

With Rust:

```
rustc shape.rs && ./shape
```
