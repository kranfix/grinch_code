# flutter_hooks_example

Example of hooks in flutter.

# How to use

Create the platform projects

```bash
flutter create .
```

Install dependencies:

```bash
flutter pub get
```

And run:

```bash
flutter run
```
